[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"

# Netlify auto-detects the Next.js App Router runtime and maps API routes
# and server-side rendering to Netlify functions automatically.
