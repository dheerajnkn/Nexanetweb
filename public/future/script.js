const $=(s,r=document)=>r.querySelector(s);const $$=(s,r=document)=>[...r.querySelectorAll(s)];
const feed=$('#messageFeed'),composer=$('#composer'),sendBtn=$('#sendBtn'),typing=$('#typingRow'),drawer=$('#threadDrawer'),threadContent=$('#threadContent'),threadTitle=$('#threadTitle');
const channelCopy={"f1-general":["f1-general","One destination for real student questions and real experiences."],jobs:["jobs-and-hiring","Openings, interviews, employers and recruiter experiences."],stem:["opt-stem","OPT, STEM OPT, E-Verify and work authorization discussions."],schools:["schools-dso","Universities, DSOs, CPT and student process discussions."]};
$$('.channel').forEach(btn=>btn.addEventListener('click',()=>{$$('.channel').forEach(b=>b.classList.remove('active'));btn.classList.add('active');const [t,d]=channelCopy[btn.dataset.channel]||[btn.dataset.channel,''];$('#channelTitle').textContent=t;$('.conversation-title p').textContent=d;}));
$$('.rail-btn').forEach(btn=>btn.addEventListener('click',()=>{$$('.rail-btn').forEach(b=>b.classList.remove('active'));btn.classList.add('active');}));
function escapeHtml(str){return str.replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));}
function bindMessage(el){el.addEventListener('click',e=>{if(e.target.closest('button')&&!e.target.classList.contains('thread-trigger'))return;openThread(el);});const trigger=$('.thread-trigger',el);if(trigger)trigger.addEventListener('click',e=>{e.stopPropagation();openThread(el);});$$('.reaction-dock button:not(.thread-trigger)',el).forEach(b=>b.addEventListener('click',e=>{e.stopPropagation();b.classList.toggle('reacted');}));}
function sendMessage(){const text=composer.value.trim();if(!text)return;const el=document.createElement('article');el.className='message outgoing spring';el.dataset.author='You';el.innerHTML=`<div class="avatar"><span>DJ</span><i></i></div><div class="message-body"><div class="message-meta"><strong>You</strong><time>now</time></div><p>${escapeHtml(text).replace(/\n/g,'<br>')}</p><div class="reaction-dock"><button>👍</button><button>💡</button><button>❤️</button><button class="thread-trigger">↗ Thread</button></div></div>`;feed.appendChild(el);bindMessage(el);composer.value='';composer.style.height='auto';feed.scrollTop=feed.scrollHeight;typing.style.opacity='1';setTimeout(()=>typing.style.opacity='.35',900);setTimeout(()=>typing.style.opacity='1',1800);}
sendBtn.addEventListener('click',sendMessage);composer.addEventListener('keydown',e=>{if(e.key==='Enter'&&!e.shiftKey){e.preventDefault();sendMessage();}});composer.addEventListener('input',()=>{composer.style.height='auto';composer.style.height=Math.min(composer.scrollHeight,140)+'px';});
$('#attachmentBtn').addEventListener('click',()=>$('#attachmentDrawer').classList.toggle('open'));$$('#attachmentDrawer button').forEach(b=>b.addEventListener('click',()=>{composer.value+=(composer.value?'\n':'')+b.dataset.insert;composer.focus();$('#attachmentDrawer').classList.remove('open');}));
$('#codeBtn').addEventListener('click',()=>{composer.value+=(composer.value?'\n':'')+'```\n\n```';composer.focus();});$('#voiceBtn').addEventListener('click',e=>{e.currentTarget.textContent=e.currentTarget.textContent==='◉'?'■':'◉';});$('#canvasBtn').addEventListener('click',()=>{$('#attachmentDrawer').classList.add('open');});
function openThread(message){threadTitle.textContent=(message.dataset.author||'Discussion')+' thread';threadContent.innerHTML='';const copy=document.createElement('div');copy.className='thread-card';copy.innerHTML=`<strong>${escapeHtml(message.dataset.author||'User')}</strong><p>${$('.message-body p',message)?.innerHTML||''}</p>`;threadContent.appendChild(copy);drawer.classList.add('open');drawer.setAttribute('aria-hidden','false');}
$('#closeThread').addEventListener('click',()=>{drawer.classList.remove('open');drawer.setAttribute('aria-hidden','true');});$('#threadSend').addEventListener('click',sendThread);$('#threadInput').addEventListener('keydown',e=>{if(e.key==='Enter')sendThread();});function sendThread(){const input=$('#threadInput'),text=input.value.trim();if(!text)return;const card=document.createElement('div');card.className='thread-card spring';card.innerHTML=`<strong>You</strong><p>${escapeHtml(text)}</p>`;threadContent.appendChild(card);input.value='';threadContent.scrollTop=threadContent.scrollHeight;}
$$('.message').forEach(bindMessage);setInterval(()=>{typing.style.opacity=typing.style.opacity==='.35'?'1':'.35';},3500);
